#
# Licensed to the Apache Software Foundation (ASF) under one or more
# contributor license agreements.  See the NOTICE file distributed with
# this work for additional information regarding copyright ownership.
# The ASF licenses this file to You under the Apache License, Version 2.0
# (the "License"); you may not use this file except in compliance with
# the License.  You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
name: Native Build Server

on:
  push:
    branches: [ test*, "*.*.*", 2.x ]
  pull_request:
    branches: [ 2.x, develop, master ]
    paths:
      - '.github/workflows/native-server.yml'
      - 'build/pom.xml'
      - 'pom.xml'
      - 'common/pom.xml'
      - 'common/src/main/**'
      - 'config/pom.xml'
      - 'config/seata-config-core/pom.xml'
      - 'config/seata-config-core/src/main/**'
      - 'config/seata-config-custom/pom.xml'
      - 'config/seata-config-custom/src/main/**'
      - 'config/seata-config-apollo/pom.xml'
      - 'config/seata-config-apollo/src/main/**'
      - 'config/seata-config-nacos/pom.xml'
      - 'config/seata-config-nacos/src/main/**'
      - 'config/seata-config-zk/pom.xml'
      - 'config/seata-config-zk/src/main/**'
      - 'config/seata-config-consul/pom.xml'
      - 'config/seata-config-consul/src/main/**'
      - 'config/seata-config-etcd3/pom.xml'
      - 'config/seata-config-etcd3/src/main/**'
      - 'config/seata-config-spring-cloud/pom.xml'
      - 'config/seata-config-spring-cloud/src/main/**'
      - 'threadpool/pom.xml'
      - 'threadpool/src/main/**'
      - 'discovery/pom.xml'
      - 'discovery/seata-discovery-core/pom.xml'
      - 'discovery/seata-discovery-core/src/main/**'
      - 'core/pom.xml'
      - 'core/src/main/**'
      - 'discovery/seata-discovery-custom/pom.xml'
      - 'discovery/seata-discovery-custom/src/main/**'
      - 'discovery/seata-discovery-consul/pom.xml'
      - 'discovery/seata-discovery-consul/src/main/**'
      - 'discovery/seata-discovery-eureka/pom.xml'
      - 'discovery/seata-discovery-eureka/src/main/**'
      - 'discovery/seata-discovery-nacos/pom.xml'
      - 'discovery/seata-discovery-nacos/src/main/**'
      - 'discovery/seata-discovery-redis/pom.xml'
      - 'discovery/seata-discovery-redis/src/main/**'
      - 'discovery/seata-discovery-sofa/pom.xml'
      - 'discovery/seata-discovery-sofa/src/main/**'
      - 'discovery/seata-discovery-raft/pom.xml'
      - 'discovery/seata-discovery-raft/src/main/**'
      - 'discovery/seata-discovery-zk/pom.xml'
      - 'discovery/seata-discovery-zk/src/main/**'
      - 'discovery/seata-discovery-etcd3/pom.xml'
      - 'discovery/seata-discovery-etcd3/src/main/**'
      - 'discovery/seata-discovery-namingserver/pom.xml'
      - 'discovery/seata-discovery-namingserver/src/main/**'
      - 'tm/pom.xml'
      - 'tm/src/main/**'
      - 'extensions/pom.xml'
      - 'extensions/rpc/pom.xml'
      - 'extensions/rpc/seata-rpc-core/pom.xml'
      - 'extensions/rpc/seata-rpc-core/src/main/**'
      - 'extensions/rpc/seata-brpc/pom.xml'
      - 'extensions/rpc/seata-brpc/src/main/**'
      - 'extensions/rpc/seata-http/pom.xml'
      - 'extensions/rpc/seata-http/src/main/**'
      - 'extensions/rpc/seata-http-jakarta/pom.xml'
      - 'extensions/rpc/seata-http-jakarta/src/main/**'
      - 'extensions/rpc/seata-dubbo-alibaba/pom.xml'
      - 'extensions/rpc/seata-dubbo-alibaba/src/main/**'
      - 'extensions/rpc/seata-sofa-rpc/pom.xml'
      - 'extensions/rpc/seata-sofa-rpc/src/main/**'
      - 'extensions/rpc/seata-motan/pom.xml'
      - 'extensions/rpc/seata-motan/src/main/**'
      - 'rm/pom.xml'
      - 'rm/src/main/**'
      - 'sqlparser/pom.xml'
      - 'sqlparser/seata-sqlparser-core/pom.xml'
      - 'sqlparser/seata-sqlparser-core/src/main/**'
      - 'compressor/pom.xml'
      - 'compressor/seata-compressor-gzip/pom.xml'
      - 'compressor/seata-compressor-gzip/src/main/**'
      - 'compressor/seata-compressor-bzip2/pom.xml'
      - 'compressor/seata-compressor-bzip2/src/main/**'
      - 'compressor/seata-compressor-zip/pom.xml'
      - 'compressor/seata-compressor-zip/src/main/**'
      - 'compressor/seata-compressor-lz4/pom.xml'
      - 'compressor/seata-compressor-lz4/src/main/**'
      - 'compressor/seata-compressor-deflater/pom.xml'
      - 'compressor/seata-compressor-deflater/src/main/**'
      - 'compressor/seata-compressor-zstd/pom.xml'
      - 'compressor/seata-compressor-zstd/src/main/**'
      - 'compressor/seata-compressor-all/pom.xml'
      - 'compressor/seata-compressor-all/src/main/**'
      - 'sqlparser/seata-sqlparser-druid/pom.xml'
      - 'sqlparser/seata-sqlparser-druid/src/main/**'
      - 'json-common/pom.xml'
      - 'json-common/json-common-core/pom.xml'
      - 'json-common/json-common-core/src/main/**'
      - 'rm-datasource/pom.xml'
      - 'rm-datasource/src/main/**'
      - 'serializer/pom.xml'
      - 'serializer/seata-serializer-seata/pom.xml'
      - 'serializer/seata-serializer-seata/src/main/**'
      - 'serializer/seata-serializer-protobuf/pom.xml'
      - 'serializer/seata-serializer-protobuf/src/main/**'
      - 'serializer/seata-serializer-kryo/pom.xml'
      - 'serializer/seata-serializer-kryo/src/main/**'
      - 'serializer/seata-serializer-hessian/pom.xml'
      - 'serializer/seata-serializer-hessian/src/main/**'
      - 'serializer/seata-serializer-fastjson2/pom.xml'
      - 'serializer/seata-serializer-fastjson2/src/main/**'
      - 'serializer/seata-serializer-fory/pom.xml'
      - 'serializer/seata-serializer-fory/src/main/**'
      - 'serializer/seata-serializer-all/pom.xml'
      - 'serializer/seata-serializer-all/src/main/**'
      - 'integration-tx-api/pom.xml'
      - 'integration-tx-api/src/main/**'
      - 'tcc/pom.xml'
      - 'tcc/src/main/**'
      - 'extensions/messaging/pom.xml'
      - 'extensions/messaging/seata-rocketmq/pom.xml'
      - 'extensions/messaging/seata-rocketmq/src/main/**'
      - 'sqlparser/seata-sqlparser-antlr/pom.xml'
      - 'sqlparser/seata-sqlparser-antlr/src/main/**'
      - 'spring/seata-spring/pom.xml'
      - 'spring/seata-spring/src/main/**'
      - 'extensions/rpc/seata-grpc/pom.xml'
      - 'extensions/rpc/seata-grpc/src/main/**'
      - 'extensions/rpc/seata-hsf/pom.xml'
      - 'extensions/rpc/seata-hsf/src/main/**'
      - 'saga/pom.xml'
      - 'saga/seata-saga-processctrl/pom.xml'
      - 'saga/seata-saga-processctrl/src/main/**'
      - 'saga/seata-saga-statelang/pom.xml'
      - 'saga/seata-saga-statelang/src/main/**'
      - 'saga/seata-saga-engine-store/pom.xml'
      - 'saga/seata-saga-engine-store/src/main/**'
      - 'saga/seata-saga-engine/pom.xml'
      - 'saga/seata-saga-engine/src/main/**'
      - 'saga/seata-saga-rm/pom.xml'
      - 'saga/seata-saga-rm/src/main/**'
      - 'saga/seata-saga-spring/pom.xml'
      - 'saga/seata-saga-spring/src/main/**'
      - 'saga/seata-saga-annotation/pom.xml'
      - 'saga/seata-saga-annotation/src/main/**'
      - 'metrics/pom.xml'
      - 'metrics/seata-metrics-api/pom.xml'
      - 'metrics/seata-metrics-api/src/main/**'
      - 'compatible/pom.xml'
      - 'compatible/src/main/**'
      - 'all/pom.xml'
      - 'all/src/main/**'
      - 'config/seata-config-all/pom.xml'
      - 'config/seata-config-all/src/main/**'
      - 'discovery/seata-discovery-all/pom.xml'
      - 'discovery/seata-discovery-all/src/main/**'
      - 'spring/seata-spring-autoconfigure/pom.xml'
      - 'spring/seata-spring-autoconfigure/seata-spring-autoconfigure-core/pom.xml'
      - 'spring/seata-spring-autoconfigure/seata-spring-autoconfigure-core/src/main/**'
      - 'spring/seata-spring-autoconfigure/seata-spring-autoconfigure-server/pom.xml'
      - 'spring/seata-spring-autoconfigure/seata-spring-autoconfigure-server/src/main/**'
      - 'metrics/seata-metrics-core/pom.xml'
      - 'metrics/seata-metrics-core/src/main/**'
      - 'metrics/seata-metrics-registry-compact/pom.xml'
      - 'metrics/seata-metrics-registry-compact/src/main/**'
      - 'metrics/seata-metrics-exporter-prometheus/pom.xml'
      - 'metrics/seata-metrics-exporter-prometheus/src/main/**'
      - 'metrics/seata-metrics-all/pom.xml'
      - 'metrics/seata-metrics-all/src/main/**'
      - 'threadpool-loom/pom.xml'
      - 'threadpool-loom/src/main/**'
      - 'server/pom.xml'
      - 'server/src/main/**'
      - 'test-suite/test-native-server/pom.xml'
      - 'test-suite/test-native-server/src/**'
      - 'test-suite/test-native-metadata-merge/pom.xml'
      - 'test-suite/test-native-metadata-merge/src/**'
  workflow_call:
    inputs:
      repository:
        description: 'Repository to check out'
        type: string
        required: false
        default: ''
      ref:
        description: 'Branch or tag to check out'
        type: string
        required: false
        default: ''

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  build:
    name: build & test (file)
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-24.04, ubuntu-24.04-arm, macos-26, windows-latest ]
        # Note: macos-26 is Apple Silicon (ARM64) only.
        # Intel macOS (x86_64) is not supported because GraalVM has
        # discontinued updates for that platform.
        # https://github.com/graalvm/graalvm-ce-builds/releases/tag/jdk-25.0.1
    env:
      USERNAME: seata
      PASSWORD: seata
    steps:
      - name: "Checkout"
        uses: actions/checkout@v7.0.0
        with:
          repository: ${{ inputs.repository || github.repository }}
          ref: ${{ inputs.ref || github.ref }}
      - name: "Start MySQL via Docker (Linux only)"
        if: runner.os == 'Linux'
        shell: sh
        run: |
          docker run -d --name mysql-test \
            -e MYSQL_ALLOW_EMPTY_PASSWORD=yes \
            -e MYSQL_DATABASE=seata_test_native \
            -p 3306:3306 \
            mysql:8.0
          echo "Waiting for MySQL to be ready..."
          for i in $(seq 1 30); do
            if docker exec mysql-test mysqladmin ping -h localhost --silent 2>/dev/null; then
              echo "MySQL is ready"
              docker logs mysql-test --tail 10
              exit 0
            fi
            echo "Waiting for MySQL... ($i/30)"
            sleep 2
          done
          echo "ERROR: MySQL failed to start after 60s"
          docker logs mysql-test --tail 50
          exit 1
      - name: "Set up Java JDK"
        uses: actions/setup-java@v5.5.0
        with:
          distribution: 'graalvm'
          java-version: 25
      - name: "Print maven version"
        run: ./mvnw -version
      - name: "Restore local maven repository cache"
        uses: actions/cache/restore@v6.1.0
        id: cache-maven-repository
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-${{ runner.arch }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-maven-
      - name: "Install dependencies"
        # test-native-server requires seata-spring-boot-starter
        run: ./mvnw -T 4C clean -B -pl server,spring/seata-spring-boot-starter install -DskipTests -am
      - name: "Compile native image"
        shell: sh
        run: |
          ./mvnw -T 2C clean -B -pl server package -DskipTests -Pnative spring-boot:process-aot native:compile
          rm -rf server/target/*.jar
      - name: "Upload binaries"
        uses: actions/upload-artifact@v7.0.1
        with:
          name: server-native-${{ runner.os }}-${{ runner.arch }}
          path: |
            server/target/seata-*-*
          if-no-files-found: error
      - name: "Verify native binary"
        shell: sh
        env:
          SEATA_CONFIG_TYPE: file
          SEATA_REGISTRY_TYPE: file
          SEATA_STORE_TYPE: file
        run: |
          ./server/target/seata-server-* &
          NATIVE_PID=$!
          echo "$NATIVE_PID" > ./seata-native-server.pid
          echo "Native binary started with PID=$NATIVE_PID"
          sleep 10
          if kill -0 $NATIVE_PID 2>/dev/null; then
            echo "Native binary is running normally (PID=$NATIVE_PID)"
          else
            echo "ERROR: Native binary failed to start or crashed (PID=$NATIVE_PID)"
            exit 1
          fi
      - name: "Check port 8091 for server in ${{ matrix.os }}"
        shell: sh
        env:
          MAX_RETRIES: 6
          SLEEP_SECONDS: 5
        run: |
          NATIVE_PID=$(cat ./seata-native-server.pid)
          echo "NATIVE_PID=$NATIVE_PID"
          i=1
          while [ $i -le $MAX_RETRIES ]; do
            if curl -sSf http://localhost:8091/health >/dev/null 2>&1; then
              echo "Port 8091 is responding"
              exit 0
            fi
            echo "Waiting for port 8091... ($i/$MAX_RETRIES)"
            sleep $SLEEP_SECONDS
            i=$((i + 1))
          done
          echo "ERROR: Port 8091 is not responding after $((MAX_RETRIES * SLEEP_SECONDS)) seconds"
          exit 1
      - name: "Start test-native-server app for server in ${{ matrix.os }}"
        if: runner.os == 'Linux'
        env:
          MAX_RETRIES: 12
          SLEEP_SECONDS: 5
        run: |
          nohup ./mvnw -e -T 2C -Ptest-native-server -pl test-suite/test-native-server spring-boot:run &
          i=1
          while [ $i -le $MAX_RETRIES ]; do
            if curl -sSf http://localhost:50180/actuator/health >/dev/null 2>&1; then
              echo "Port 50180 is responding"
              exit 0
            fi
            echo "Waiting for port 50180... ($i/$MAX_RETRIES)"
            sleep $SLEEP_SECONDS
            i=$((i + 1))
          done
          echo "ERROR: Port 50180 is not responding after $((MAX_RETRIES * SLEEP_SECONDS)) seconds"
          exit 1
      - name: "Run native tests for server in ${{ matrix.os }}"
        if: runner.os == 'Linux'
        run: ./mvnw -e -T 2C -Ptest-native-server -pl test-suite/test-native-server clean test
      - name: "Capture test reports on failure"
        if: failure() && runner.os == 'Linux'
        shell: sh
        run: |
          echo "=== Surefire Test Reports ==="
          if [ -d test-suite/test-native-server/target/surefire-reports ]; then
            for f in test-suite/test-native-server/target/surefire-reports/*.txt; do
              [ -f "$f" ] || continue
              echo ""
              echo "============================================================"
              echo "--- $f ---"
              echo "============================================================"
              cat "$f"
            done
          else
            echo "No surefire-reports directory found"
          fi
      - name: "Delete snapshots from the Maven local repository"
        shell: sh
        run: find ~/.m2/repository -type d -name '*-SNAPSHOT' -print -exec rm -r {} + || true
      - name: "Save local maven repository cache"
        uses: actions/cache/save@v6.1.0
        if: steps.cache-maven-repository.outputs.cache-hit != 'true'
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-${{ runner.arch }}-maven-${{ hashFiles('**/pom.xml') }}

  nacos:
    name: test (nacos)
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # Mac、Windows 没有 Docker，无法创建 Nacos 容器，跳过 macos-26, windows-latest
        os: [ ubuntu-24.04, ubuntu-24.04-arm ]
    steps:
      - name: "Checkout"
        uses: actions/checkout@v7.0.0
        with:
          repository: ${{ inputs.repository || github.repository }}
          ref: ${{ inputs.ref || github.ref }}
      - name: "Start MySQL via Docker (Linux only)"
        if: runner.os == 'Linux'
        shell: sh
        run: |
          docker run -d --name mysql-test \
            -e MYSQL_ALLOW_EMPTY_PASSWORD=yes \
            -e MYSQL_DATABASE=seata_test_native \
            -p 3306:3306 \
            mysql:8.0
          echo "Waiting for MySQL to be ready..."
          for i in $(seq 1 30); do
            if docker exec mysql-test mysqladmin ping -h localhost --silent 2>/dev/null; then
              echo "MySQL is ready"
              docker logs mysql-test --tail 10
              exit 0
            fi
            echo "Waiting for MySQL... ($i/30)"
            sleep 2
          done
          echo "ERROR: MySQL failed to start after 60s"
          docker logs mysql-test --tail 50
          exit 1
      - name: "Set up Java JDK"
        uses: actions/setup-java@v5.5.0
        with:
          distribution: 'graalvm'
          java-version: 25
      - name: "Print maven version"
        run: ./mvnw -version
      - name: "Restore local maven repository cache"
        uses: actions/cache/restore@v6.1.0
        id: cache-maven-repository
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-${{ runner.arch }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-maven-
      - name: "Install dependencies"
        # test-native-server requires seata-spring-boot-starter
        run: ./mvnw -T 4C clean -B -pl server,spring/seata-spring-boot-starter install -DskipTests -am
      - name: "Download binaries"
        uses: actions/download-artifact@v7.0.0
        with:
          name: server-native-${{ runner.os }}-${{ runner.arch }}
          path: server/target/
      - name: "Verify native binary"
        shell: sh
        env:
          SEATA_CONFIG_TYPE: nacos
          SEATA_CONFIG_NACOS_SERVERADDR: "127.0.0.1:8848"
          SEATA_CONFIG_NACOS_NAMESPACE: ""
          SEATA_CONFIG_NACOS_GROUP: SEATA_GROUP
          SEATA_CONFIG_NACOS_CONTEXTPATH: ""
          SEATA_CONFIG_NACOS_USERNAME: ""
          SEATA_CONFIG_NACOS_PASSWORD: ""
          SEATA_CONFIG_NACOS_ACCESSKEY: ""
          SEATA_CONFIG_NACOS_SECRETKEY: ""
          SEATA_CONFIG_NACOS_RAMROLENAME: ""
          SEATA_CONFIG_NACOS_DATAID: seataServer.properties
          SEATA_REGISTRY_TYPE: nacos
          SEATA_REGISTRY_PREFERREDNETWORKS: "30.240.*"
          SEATA_REGISTRY_IGNOREDINTERFACES: VMware.*
          SEATA_REGISTRY_METADATA_WEIGHT: "100"
          SEATA_REGISTRY_NACOS_APPLICATION: seata-server
          SEATA_REGISTRY_NACOS_SERVERADDR: "127.0.0.1:8848"
          SEATA_REGISTRY_NACOS_NAMESPACE: ""
          SEATA_REGISTRY_NACOS_GROUP: "SEATA_GROUP"
          SEATA_REGISTRY_NACOS_CLUSTER: default
          SEATA_REGISTRY_NACOS_CONTEXTPATH: ""
          SEATA_REGISTRY_NACOS_USERNAME: ""
          SEATA_REGISTRY_NACOS_PASSWORD: ""
          SEATA_REGISTRY_NACOS_ACCESSKEY: ""
          SEATA_REGISTRY_NACOS_SECRETKEY: ""
          SEATA_REGISTRY_NACOS_RAMROLENAME: ""
          SEATA_STORE_TYPE: file
        run: |
          ./server/target/seata-server-* &
          NATIVE_PID=$!
          echo "$NATIVE_PID" > ./seata-native-server.pid
          echo "Native binary started with PID=$NATIVE_PID"
          sleep 10
          if kill -0 $NATIVE_PID 2>/dev/null; then
            echo "Native binary is running normally (PID=$NATIVE_PID)"
          else
            echo "ERROR: Native binary failed to start or crashed (PID=$NATIVE_PID)"
            exit 1
          fi
      - name: "Check port 8091 for server in ${{ matrix.os }}"
        shell: sh
        env:
          MAX_RETRIES: 6
          SLEEP_SECONDS: 5
        run: |
          NATIVE_PID=$(cat ./seata-native-server.pid)
          echo "NATIVE_PID=$NATIVE_PID"
          i=1
          while [ $i -le $MAX_RETRIES ]; do
            if curl -sSf http://localhost:8091/health >/dev/null 2>&1; then
              echo "Port 8091 is responding"
              exit 0
            fi
            echo "Waiting for port 8091... ($i/$MAX_RETRIES)"
            sleep $SLEEP_SECONDS
            i=$((i + 1))
          done
          echo "ERROR: Port 8091 is not responding after $((MAX_RETRIES * SLEEP_SECONDS)) seconds"
          exit 1
